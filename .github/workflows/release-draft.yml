name: "Release"
on: workflow_dispatch
jobs:
  draft:
    uses: gmvrpw/t15i/.github/workflows/nodejs-lib-release-draft.yml@main
    with: 
      target: ${{ github.event.repository.default_branch }}
      semver-bump-labels: breaking.feature.fix

